# dsh-agent-team-model profile patch layer — insert one loader entry for this
# plugin package. The entry is a normal cordis loader row (id + package name);
# the browser half is picked up by dsh-client-modules through the package's
# `dsh.client` declaration, exactly like the shipped ui-* packages.
- insert:
    - id: agent-team-model
      name: 'dsh-agent-team-model'
